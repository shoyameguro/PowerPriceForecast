# conf/tune.yaml
defaults:
  - _self_

# ---- 対象モデル設定とデータ --------------------------------------------
cfg: src/config/lgbm_baseline.yaml
input: data/train/train.pkl

# ---- Optuna 設定 --------------------------------------------------------
trials: 100
out: ${hydra:runtime.output_dir}/tuning

hydra:
  run:
    dir: outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}

